## Discovering Live Hosts and Open Ports

### Minimum Requirements

1. Attacker machine (Kali/Ubuntu/Windows)
2. 1–2 target machines (VMs or real devices)

```bash
sudo apt update
sudo apt install ipscan
```
